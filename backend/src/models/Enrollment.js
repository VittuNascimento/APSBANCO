class Enrollment {
    constructor(id, student, course, enrollmentDate) {
      this.id = id;
      this.student = student;
      this.course = course;
      this.enrollmentDate = enrollmentDate;
    }
  }
  
  module.exports = Enrollment;
  